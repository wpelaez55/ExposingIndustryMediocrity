package ny0k;

import java.util.concurrent.TimeUnit;

final class an implements Runnable {
    an() {
    }

    public final void run() {
        al.f3602m.closeIdleConnections(0, TimeUnit.MILLISECONDS);
    }
}
